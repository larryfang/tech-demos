# Optional. Fixture stubs work with no key.
# When set, check "Use live vision" to call an OpenAI-compatible
# POST /v1/chat/completions vision model. Missing or failing calls
# fall back to the fixture stub.
OPENAI_API_KEY=

# Optional overrides for compatible providers.
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_VISION_MODEL=gpt-4o-mini
